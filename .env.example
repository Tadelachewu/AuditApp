# This is an example .env file.
# Copy this to a new file named .env and fill in your actual values.

# The connection string for your PostgreSQL database.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/x-audit"

# A secret key for signing session cookies.
# Generate a secure random string for this (e.g., `openssl rand -base64 32`).
SESSION_SECRET="a_very_secure_secret_for_local_development_change_me"

# Optional: Your Google AI API key if you are using Genkit with Google AI.
# Get one from Google AI Studio: https://aistudio.google.com/app/apikey
# GOOGLE_API_KEY=""
